在实现 Phase 4 之前，请先确认以下五点，不要直接开始写代码：

1. "仅当前活跃阶段可发送消息，非活跃阶段返回 403"这条规则，
   请说明它是否会阻断已经设计好的话题级回溯功能（Task 2.8 的
   revalidateStage()、canEnterStage()）——话题级回溯的前提是
   用户能够重新进入一个已完成、非当前活跃的阶段并触发新的对话。
   请明确这条 403 规则的适用范围：是所有非活跃阶段一律禁止交互，
   还是应该区分"纯历史只读阶段"和"因回溯/失效而被重新标记为
   可交互的阶段"这两种情况，分别处理。

2. invalidated 阶段的失效标识，"点击查看原有对话 + 提示建议重新
   运行"，请明确这个"提示"是否应该直接提供一个"重新运行此阶段"
   的可点击操作，联动调用已有的 reExecuteStage() 接口，而不是
   仅展示一段建议性文字。

3. 4.2 审计卡片里"智能优化"操作完成后，请确认新生成的内容是否
   会重新展示给用户确认（这是此前已经定好的原则：AI 重新生成后
   必须重新展示，不能静默替换直接推进）。请说明在新的 8-Tab
   导航结构下，这个"重新展示确认"具体如何呈现，是否需要新增
   界面元素。

4. "报告不展示审计评分/审计历史/对话过程"这条规则，请确认它
   仅针对最终导出的 PDF 报告正文，而不影响报告在线编辑界面
   （创始人编辑时应该能看到相关的字段级审计反馈）。请明确区分
   这两个界面各自的展示范围。

5. 4.4 文件上传与图片粘贴功能，请说明具体的使用场景（用在哪个
   阶段、什么用途），我需要先确认这个功能的必要性和范围，
   再决定是否本次一并实现。

请先逐条回答，我确认后再进入具体实现。



现在开始实现 Phase 4，按 4.1 → 4.2 → 4.3 → 4.4 的顺序，
每完成一块，给我看结果，确认后再进行下一块，不要一次性全部做完。

以下是本次确认过的五项前提，作为强制实现要求，不要偏离：

1. "非活跃阶段发消息返回 403"这条规则，仅适用于纯历史只读、
   从未被回溯或标记失效的阶段。已通过话题级回溯或因上游变更
   被标记为 invalidated 的阶段，需要保持可交互，403 校验逻辑
   必须先检查该阶段当前状态（是否处于可重新进入的状态），
   而不是简单按"是否为当前活跃阶段编号"一刀切判断。

2. invalidated 阶段的失效标识，点击后除了展示原有对话历史，
   必须直接提供一个"重新运行此阶段"的可点击按钮，联动调用
   已有的 reExecuteStage() 接口，不能只是一段建议性文字。

3. 审计卡片"智能优化"操作完成后，重新生成的内容必须重新展示
   给用户确认，不能静默替换直接推进到下一阶段。在 8-Tab 导航
   结构下，这个重新确认应该在当前 Tab 内直接呈现更新后的内容
   和确认按钮，不跳转到其他界面。

4. "报告不展示审计评分/审计历史/对话过程"这条规则，只适用于
   最终导出的 PDF 报告正文。报告在线编辑界面（富文本编辑模式）
   允许展示字段级的审计反馈，这两个界面的展示范围需要在代码里
   明确区分，不能用同一套渲染逻辑处理。

5. 文件上传与图片粘贴功能，用于 [请在这里填入你确认的具体使用
   场景，例如：Stage 1 用户访谈阶段，允许创始人上传产品参考图/
   已有设计稿，作为访谈素材的一部分]。如果这个场景和你的实际
   决定不一致，请先停下来跟我确认，不要按默认猜测的场景实现。

---

第一步：实现 4.1（品牌咨询工作台）

先完成 StageTabs / StageView / StageGoal / StageProgress 四个
新增组件，以及 ChatView / InputArea / StageSidebar / TopBar 的
对应修改。API 层先完成 POST message 的活跃阶段校验（按上述
第 1 点的范围）和 GET stage 的 goal + progress 字段扩展。

完成后，请提供一份手动验证清单，覆盖以下场景，我会自己过一遍：
- 正常切换 8 个 Tab，对话历史和状态是否正确隔离
- 刷新页面后，状态是否正确从 DB 恢复，而非依赖 localStorage
- 尝试对一个纯历史只读阶段发消息，确认返回 403
- 尝试对一个被标记 invalidated 的阶段发消息，确认不会被 403
  拦截，能正常触发 reExecuteStage()
- SSE 连接中断后重新打开页面，已接收的片段是否正确恢复

完成并通过验证后，再进入第二步。

---

第二步：实现 4.2（审计卡片 UI）

Advance / Reoptimize / Block 三态卡片，以及"智能优化"/"手动
调整"/"保持当前决策"三个操作，按上述第 3 点的要求实现"重新
确认"流程。完成后给我看效果，确认后进入第三步。

---

第三步：实现 4.3（品牌战略报告页 UI）

八章节折叠 + 在线编辑 + PDF 导出按钮，按上述第 4 点的要求区分
两个界面的审计信息展示范围。完成后给我看效果，确认后进入第四步。

---

第四步：实现 4.4（文件上传与图片粘贴）

按上述第 5 点确认的具体场景实现，如果场景描述不清楚，请先向我
确认，不要自行假设范围后直接实现。