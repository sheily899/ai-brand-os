---
name: e2e-test-phase1-2-2026-08-01
description: 2026-08-01 Phase1/2 端到端测试结果 — 5案例 S1→S8 完整链路，记录所有发现的问题及优化建议
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e1cbe48-91f8-40c6-a5e6-84435abb7388
---

# Phase 1/2 端到端测试结果（2026-08-01）

## 测试概况

- 测试范围：5 个案例 × S1→S8 完整链路
- 测试脚本：`scripts/run-batch.ts`
- 测试案例：`reference/brand-domain-cases.md`
- 总轮次：210 轮对话 + 40 次 Convergence + 40 次 Advance
- 总耗时：~65 分钟 for 5 cases, ~13 min/case
- LLM Provider: DeepSeek (deepseek-chat)
- Search API: 博查 Search（全部超时，见 P0）

## 核心结论

Phase 1/2 Pipeline 基础设施已稳定可用。40/40 阶段 Converge + Advance 100% 通过。
Search Intelligence Layer 因 博查 API 从中国网络不可达而无法验证。

---

## P0 · 博查 Search 完全不可用（网络限制）

- **发现时间**：2026-08-01
- **现象**：S2/S3/S5/S8 所有搜索请求均超时，约 130 次 `brave-search` 调用全部失败
- **根因**：`api.search.brave.com` 从中国网络环境无法访问（curl 15s 超时验证确认）
- **影响**：搜索阶段 Opening Message 缺少实时行业数据支撑，仅依赖 Decision Memory + LLM 知识
- **优化建议**：
  1. 接入国内可搜索 API（Bing Search API / SerpAPI 中国代理）
  2. 代码增加 HTTP_PROXY 支持
  3. 短期：将超时从 10s 缩到 5s，减少无效等待时间

## P1 · S3 Convergence 校验失败率高

- **发现时间**：2026-08-01
- **现象**：5 个案例中 4 个 S3 需要 2-3 次 retry（共 9 次额外 LLM 调用），S1/S2/S4/S6/S7/S8 几乎全部 0 retry
- **根因**：S3 Schema 字段最多且嵌套深（marketOverview/industryTrend/channelAnalysis/categoryStatus/experienceGaps/opportunityDirections），AI 首次输出易遗漏子字段（如 `evidenceLevel`、`currentAlternative`）
- **影响**：每个 retry 额外消耗 ~800 tokens
- **优化建议**：
  1. S3 Convergence Prompt 显式列出所有必填子字段
  2. 或将部分深层必填字段改为 optional + default，降低 schema 严格度

## P1 · 阶段间搜索上下文不延续

- **发现时间**：2026-08-01
- **现象**：`advanceToNextStage()` 的搜索结果仅注入首轮 Opening Message，第 2 轮起 consultation 对话丢失搜索上下文
- **根因**：
  1. `AdvanceResult` 未返回 `searchContext` 字段
  2. batch 脚本 `sendMessage()` 调用未传 `searchContext`
- **影响**：S2/S3/S5/S8 后续轮次 AI 无法引用搜索发现
- **优化建议**：
  1. `AdvanceResult` 增加 `searchContext: string` 字段
  2. batch 脚本 consultation 循环中沿用它

## P2 · S4 identityNeed 证据等级分类错误

- **发现时间**：2026-08-01
- **现象**：全部 5 案例 `deepNeeds.identityNeed` 被标记为 `confirmed_decision`，内容含"消费者可能希望""据其行为推测"等假设性语言
- **根因**：S4 extractor 将 `identityNeed` 归类为决策型字段，但实际是从观察推导的推断
- **影响**：降低 Decision Memory 中 confirmed_decision 分类的可靠性
- **优化建议**：S4 extractor 中 `identityNeed` 改为输出 `hypothesis`，或在内容出现"可能/推测"时自动降级

## P3 · 报告语体检测启发式过严

- **发现时间**：2026-08-01
- **现象**：AI 开场白均为专业咨询报告语体，但 `detectReportStyle()` 检出率 0/40
- **根因**：detectReportStyle formalPatterns 要求精确匹配（如"数据显示""基于以上"），AI 实际使用不同但同样专业的表达（如"我们进入""前序阶段已确认""帮你理清"）
- **影响**：不影响功能，统计指标失真
- **优化建议**：
  1. 降低阈值：score >= 3 改为 >= 2
  2. 增加 AI 实际使用的模式词

## P3 · Founder 模拟器收束信号不够自然

- **发现时间**：2026-08-01
- **现象**：部分阶段最后一轮 founder 的"可以进行收束了"过渡生硬
- **根因**：`isFinalRound` 指令强制插入收束信号，与对话上下文可能不连贯
- **影响**：轻微影响对话自然度
- **优化建议**：改为让 founder 根据对话自然程度主观判断是否可收束，而非固定触发

---

## 验证通过项

- ✅ 40/40 阶段 Converge + Advance 100% 通过
- ✅ 每阶段均在 maxRounds 内完成（S1=8, S2-5=5, S6=6, S7-8=4）
- ✅ Decision Memory 传递完整（每案例 65-72 条，S1→S6 推导链可追溯）
- ✅ S6 战略枢纽：全部 5 案例 positioning 引用 S4 identityNeed + S5 competitiveGap
- ✅ S7 视觉可追溯到 S6 brandPersonality
- ✅ S8 内容策略服务于 S6 brandPositioning
- ✅ problem_driven / creation_driven 两种创始人类型均正常
- ✅ AI 一次一问，无追问过多
- ✅ Search 失败时优雅降级
- ✅ Convergence retry 机制有效（最多 3 次）
- ✅ AI 输出均为专业分析语言，无口语化
